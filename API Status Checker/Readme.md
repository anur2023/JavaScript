# 🔍 API Status Checker & Data Viewer

A simple web application built using **HTML, CSS, and JavaScript** that demonstrates how to fetch data from a public API and display the result dynamically in a table.

This project is created to understand how **async/await**, **Fetch API**, and **DOM manipulation** work together in real-world scenarios.

---

## 📌 Project Description

This project allows the user to enter an ID, fetch data from an API, and display the returned result on the webpage.  
It also shows the current status of the API request such as:

- Loading  
- Success  
- Failed  

The fetched data is displayed inside a table format.

---

## 🚀 Features

- Fetch data from API using JavaScript  
- Uses async / await  
- Displays loading, success and failed states  
- Shows data in a table  
- Clean and simple interface  

---

## 🛠️ Technologies Used

- HTML  
- CSS  
- JavaScript  

---
